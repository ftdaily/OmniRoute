- **fix(combo):** A request-level 400 (parameter/schema validation such as `Extra inputs are not permitted`, unknown field, malformed body or context overflow) no longer cools down the connection and rotates through every sibling account that would fail identically; account-scoped 400s (auth or rate-limit text) still cool down (#13757, [#14438](https://github.com/diegosouzapw/OmniRoute/pull/14438)) — thanks @HouMinXi
