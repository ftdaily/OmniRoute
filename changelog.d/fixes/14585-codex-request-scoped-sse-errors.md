- **fix(routing):** preserve structured pre-content SSE error semantics so request-scoped `invalid_request_error` refusals remain sanitized 4xx outcomes, can fall back to a different model, and never poison account/model health or retry the same rejected request; transient server failures keep their existing retry and lockout behavior across priority, round-robin, and nested combo dispatch.
