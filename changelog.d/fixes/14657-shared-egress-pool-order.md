- **fix(proxies):** a pool member sharing a recently refused egress address is served last for providers whose quota is bucketed by egress address — order only, nobody is ever excluded, and other providers keep the plain rotation (covers refused members only; recently served members are a follow-up). ([#14657](https://github.com/diegosouzapw/OmniRoute/pull/14657)) — thanks @maxmad64bis
